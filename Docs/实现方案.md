- 技能冷却（参考视频39）：GA上Cooldown 配置一个GE，   ASC里有监听的，在Character里声明多播代理，然后蓝图监听事件显示给UI
- 冲刺 （参考视频40）：通过一个AN，施加力，给Movement组件AddImpulse ，添加一个GA，按键触发该GA，GA里播放一个动画，动画上面挂该notify， 通过tag配置攻击时不允许触发
- 大招（参考视频41）：创建一个GA播放这个动画，伤害通过动画里的AN，之前写的创建一个伤害盒子Actor来实现，然后继承这个Actor创建一个子Actor，给他挂点特效更好看。
- 药剂恢复魔法值和生命值（参考视频42）： 
    - 生命值魔法值恢复： GA播放动画 -> AN-> 通过ASC施加GE和触发GC（使用之前的跳字） -> 扣减药剂
    - 拾取和消耗药剂（视频47和48）：准备模型和DataAsset（使用C++写的TestGASPotion类，继承自DataAsset的），药剂Actor添加Sphere碰撞，播放药剂自身动画，碰到后Add到背包里，在背包里点击消耗，走背包系统里的逻辑（注意GAS的Net Execution Policy）
- 仓库（背包）
    - （视频43）：定义Item基类（继承自UPrimaryDataAsset，定义各种item基础属性）， 在AssetManager里加载资产、声明ItemType，再定义装备和药剂类TestGASPotion继承自基类（这些类里定义授权GA的类）
    - （视频44）：背包系统是一个组件，仓库改变时用单播通知，核心数据结构是一个数组增删改查和两个item位置交换， 组件挂在character上
    - （视频45）：背包UI， 基础UI是SlotBase、详情框和拖拽UI、背包UI主要是InventorySlot（继承自SlotBase，拖拽类存这里） 和InventoryPanel（汇集Slot，监听ItemChanged事件）两个文件。   使用背包里的Item从UI Clicked事件从Character中转RPC到服务端到InventoryComponent，然后拿到Item授权的GA
    - （视频49）：物品在背包里拖拽，原理是按键按下时，获取目标widget，把事件传给它。拖拽事件时，创建一个拖拽类，给它赋值图片，然后创建一个UDrapDropOperation，用它来控制，同时隐藏原图。 拖拽完成时showIcon，如果是跨背包和装备了，发rpc去服务端交换。
- 装备
    - （视频50）：装备也是个独立组件，数据结构是一个TArray，增删改查，包括交换到仓库里的接口。 添加装备到某个槽时施加GE修改玩家属性，一样的rpc都写在character里
    - （视频51）：装备UI，也是有panel和slot
    - （视频52-53）：武器拾取（UE商城资产无尽之剑）和使用卸载，也是和药瓶是一样的， 目前业务是装备武器的逻辑写在仓库的使用Item，使用时把它添加到装备组件里。
- 仓库和装备两个区域里拖拽实现（视频55-56）：   主要注意真正的交换操作要由服务端处理
    - 仓库内拖拽、装备内拖拽： 原理是一样的，在事件时创建拖拽类，给拖拽类图片，然后隐藏原本slot图片， 借助UDrapDropOperation， 放置后再做相反操作。
    - 仓库向背包拖拽、背包向仓库拖拽：在Drop事件触发时根据来slot type来判断是否跨了组件，从而找对应的组件做逻辑
- 鼠标指针显隐：controller调接口show...Mouse就好了
- 打包测试（视频58）